machine:
    node:
        version: 4.1.0
    python:
        version: 2.7.9
    services:
        - docker
    post:
        - pip install -U docker-compose==1.3.3

dependencies:
    override:
        - docker-compose up -d kafka
        - docker run -d --net=host couchbase/server
