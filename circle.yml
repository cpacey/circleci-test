machine:
    environment:
        DOCKER_HOST_IP: "`ifconfig lxcbr0 | grep -Po 'inet addr:\\K\\d+.\\d+.\\d+.\\d+' | sed 's/inet addr:\\(.+\\)/\\1/'`"

test:
    override:
        - echo $DOCKER_HOST_IP
